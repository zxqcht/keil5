#include <REGX52.H>
#include "LCD1602.H"
#include "MatrixKey.H"

unsigned char KeyNum;

void main()
{
	LCD_Init();
	LCD_ShowString(1,1,"Hello cht");
	while(1)
	{
		KeyNum=MatrixKey();
		if(KeyNum)
		{
			LCD_ShowNum(2,1,KeyNum,2);
		}
	}
}
